╔═══════════════════════════════════════════════════════════════════╗
║                                                                   ║
║   🚨 MONGODB CONNECTION TIMEOUT - ACTION REQUIRED 🚨              ║
║                                                                   ║
╚═══════════════════════════════════════════════════════════════════╝

Your MongoDB Atlas cluster is blocking connections because your IP 
address is not whitelisted.

┌───────────────────────────────────────────────────────────────────┐
│ STEP 1: Open MongoDB Atlas                                       │
└───────────────────────────────────────────────────────────────────┘

1. Go to: https://cloud.mongodb.com
2. Log in with your credentials
3. Select your project

┌───────────────────────────────────────────────────────────────────┐
│ STEP 2: Add IP to Network Access                                 │
└───────────────────────────────────────────────────────────────────┘

1. Click "Network Access" in the left sidebar
2. Click "ADD IP ADDRESS" button
3. Choose ONE of these options:

   Option A (QUICK FIX - Less Secure):
   ────────────────────────────────────
   • Click "ALLOW ACCESS FROM ANYWHERE"
   • This adds 0.0.0.0/0 to whitelist
   • ✅ Works immediately for all IPs
   • ⚠️  Less secure for production

   Option B (PRODUCTION - More Secure):
   ────────────────────────────────────
   • Get your current IP: https://whatismyipaddress.com
   • Enter your IP address
   • Add Render's IP ranges (contact Render support)
   • ✅ More secure
   • ⚠️  Need to update when IP changes

4. Click "Confirm"
5. Wait 1-2 minutes for changes to propagate

┌───────────────────────────────────────────────────────────────────┐
│ STEP 3: Verify Cluster is Active                                 │
└───────────────────────────────────────────────────────────────────┘

1. Go to "Database" in the left sidebar
2. Check your cluster status:
   • Green dot = Active ✅
   • Gray dot = Paused ❌

3. If paused, click "..." menu → "Resume"

┌───────────────────────────────────────────────────────────────────┐
│ STEP 4: Test Connection Again                                    │
└───────────────────────────────────────────────────────────────────┘

After updating Network Access, run:

   cd /Users/chris/dev/NextGem/aphrodite-separated/aphrodite-backend
   node test-mongodb-connection.js

Expected output:
   ✅ SUCCESS! MongoDB connected in XXXms

┌───────────────────────────────────────────────────────────────────┐
│ STEP 5: Deploy to Render                                         │
└───────────────────────────────────────────────────────────────────┘

Once local connection works:

1. Commit changes:
   git add .
   git commit -m "Fix MongoDB connection timeout issues"
   git push

2. Render will auto-deploy

3. Check Render logs for:
   ✅ MongoDB connected successfully

┌───────────────────────────────────────────────────────────────────┐
│ WHY THIS HAPPENED                                                 │
└───────────────────────────────────────────────────────────────────┘

MongoDB Atlas uses IP whitelisting for security:
• Render servers use dynamic/changing IP addresses
• Your local machine has a different IP
• Both must be whitelisted to connect
• Using 0.0.0.0/0 allows all IPs (convenient for development)

┌───────────────────────────────────────────────────────────────────┐
│ NEED HELP?                                                        │
└───────────────────────────────────────────────────────────────────┘

If you still get errors after whitelisting:

1. Check MongoDB Atlas status page:
   https://status.mongodb.com

2. Verify credentials in .env:
   MONGODB_URI should match exactly from Atlas

3. Free tier limitations:
   • Max 500 connections
   • Limited storage (512MB)
   • May throttle heavy usage

4. Check Render logs for specific errors

╔═══════════════════════════════════════════════════════════════════╗
║  📚 Full troubleshooting guide: MONGODB_TROUBLESHOOTING.md       ║
╚═══════════════════════════════════════════════════════════════════╝
